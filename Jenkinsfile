pipeline {
    agent {
        docker 'openjdk:17-jdk'  
    }
    stages {
        stage('Setup Container') {
            steps {
                echo 'Setting up the containerized environment for Java'
            }
        }
    }
}
